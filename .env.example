# Copy to .env.local for local development, and add the same keys in
# Vercel → Project → Settings → Environment Variables.

# --- Sanity (product admin) -------------------------------------------
# Leave blank to run the site on the built-in sample catalog.
NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production
# Secret shared with the Sanity webhook so new products appear instantly.
SANITY_REVALIDATE_SECRET=

# --- Site ---------------------------------------------------------------
# Public URL of the site (used for SEO links). Vercel sets VERCEL_URL
# automatically, so this is only needed once you have a custom domain.
NEXT_PUBLIC_SITE_URL=

# --- Payments (Phase 3) -----------------------------------------------
# "etsy" (default) sends shoppers to Etsy to pay. "stripe" enables direct
# checkout once the Stripe integration is added.
NEXT_PUBLIC_PAYMENT_PROVIDER=etsy

# --- Inbox (contact form + newsletter) ---------------------------------
# Sanity token with Editor rights (sanity.io/manage → API → Tokens).
SANITY_WRITE_TOKEN=
# Optional email alerts via resend.com
RESEND_API_KEY=
NOTIFY_EMAIL=
NOTIFY_FROM=
